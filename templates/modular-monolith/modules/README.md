# Modular Monolith Modules

Place domain modules here (auth, users, billing, projects, etc.).
Each module owns its routes, services, and data access.

Suggested layout:
```
modules/
  auth/
  users/
  billing/
  projects/
```
