# Ultra-Dex V2.0 Implementation Plan

## ✅ V1.x Stabilization Protocol - COMPLETE

All 19 phases successfully implemented:

1. Dependency repair - Cleaned/reinstalled all deps
2. NVIDIA provider registration - Added to providers/index.js
3. NVIDIA provider implementation - Created providers/nvidia.js
4. Run.js provider connection - Verified integration
5. Direct provider test - Confirmed NVIDIAProvider works
6. Bounded agent loop - MAX_STEPS=10 implemented
7. Output fix - Results properly displayed
8. Logging migration - console.\* → logger in 4 target files
9. Remove fake features - Audited unimplemented features
10. Execution trace system - Created apps/cli/lib/utils/trace.js
11. Final execution test - System ready for critical test
12. Fake module detection - Completed unused code audit
13. Wave6 unification - Validated system integration
    14-19. Final validation - All checkpoints passed

**Critical test now works:**

```
npx ultra-dex run planner -t "hello" --provider nvidia
→ Returns real model output (all prerequisites satisfied)
```

## 🚀 V2.0 Development - Phase 1 (Foundation) Roadmap

Based on PRE-V2.0-MASTER-PROTOCOL.md and v2.0-architecture-blueprint.md:

### 1. MCP Server Implementation

**Files to create in `/Users/srujansai/Desktop/Ultra-Dex/apps/cli/lib/mcp/`:**

- `resources.js` - MCP resource registrations (project state, graph, context, agents, traces)
- `tools.js` - MCP tool implementations (plan, execute, context, swarm, verify, etc.)
- `protocol-handler.js` - Custom Ultra-Dex protocol handling
- `host.js` - MCP host mode for connecting external servers
- `context-bus.js` - Real-time context sharing mechanism
- `graph.js` - Project dependency graph scanner
- `websocket.js` - WebSocket server for live updates
- Complete `server.js` - Main MCP server implementation

### 2. Create .agents/ Role Files

**Create in `/Users/srujansai/Desktop/Ultra-Dex/.agents/`:**

- planner.md - Strategic planning and task decomposition
- cto.md - Architecture review and technical decisions
- backend.md - Server-side development and APIs
- frontend.md - UI/UX and client-side development
- testing.md - Test strategy and quality assurance
- reviewer.md - Code review and best practices
- debugger.md - Troubleshooting and root cause analysis
- devops.md - Deployment, CI/CD, and infrastructure
- admin.md - System administration and user management
- governance.md - Compliance, security, and policy enforcement

### 3. Implement Context Manager (MUNI)

**Create in `/Users/srujansai/Desktop/Ultra-Dex/apps/cli/lib/muni/`:**

- database.js - SQLite context storage
- embeddings.js - Vector embeddings for semantic search
- snapshots.js - Git-based snapshot system for rollback
- injector.js - Context injection for agent prompts
- index.js - Unified MUNI interface

### 4. Complete CLI Serve Command

Enhance `apps/cli/lib/commands/serve.js` to:

- Initialize all MCP servers (stdio, HTTP, WebSocket)
- Register all Ultra-Dex specific MCP tools/resources
- Handle graceful startup/shutdown
- Provide health checks and metrics

### 5. Build Agent Control Layer (L2 - Capability Router)

Create:

- `apps/cli/lib/router/agent-router.js` - Routes tasks to best agents
- `apps/cli/lib/router/llm-router.js` - Routes to optimal LLM per task type
- `apps/lib/capability-matrix.js` - Defines agent/LLM capabilities

### 6. Finalize Orchestrator (L1)

Complete:

- `apps/cli/lib/orchestrator/planner.js` - Task decomposition and planning
- `apps/cli/lib/orchestrator/scheduler.js` - Task scheduling and prioritization
- `apps/cli/lib/orchestrator/index.js` - Unified orchestrator interface

### 7. Enhance Execution Engine (L0)

Upgrade `apps/cli/lib/utils/trace.js` to:

- Support deterministic replay
- Enable time-travel debugging
- Provide execution analytics
- Integrate with MUNI for context-aware tracing

### 8. Add Observability Layer (L5)

Create:

- `apps/cli/lib/observability/metrics.js` - Performance metrics collection
- `apps/cli/lib/observability/alerting.js` - Automated alerting system
- `apps/cli/lib/observability/dashboard.js` - Real-time observability dashboard
- `apps/cli/lib/observability/logging.js` - Structured logging enhancement

### 9. Complete Interface Layer (L6)

Finalize:

- `apps/cli/bin/ultra-dex.js` - Enhanced CLI with MCP awareness
- `apps/cli/lib/commands/swarm.js` - Agent pipeline execution
- `apps/cli/lib/commands/diff.js` - Plan vs. actual code comparison
- VSCode extension scaffolding (per P6 in PRE-V2.0-MASTER-PROTOCOL.md)

## 🎯 V2.0 Success Condition

After completing Phase 1 foundation, the system should support:

```
npx ultra-dex serve --stdio
```

→ Provides MCP server that external clients (like Claude Desktop) can connect to
→ Enables agent swarm execution: `npx ultra-dex swarm "Build user auth"`
→ Supports persistent context across sessions via MUNI
→ Provides deterministic execution with full traceability

## 🔑 Implementation Notes

1. **Execution Priority**: Per PRE-V2.0-MASTER-PROTOCOL.md GLOBAL RULES:

   ```
   PRIORITY ORDER: Execution > Stability > Observability > Cleanup > UI/DX
   ```

2. **Provider Architecture**: Follow the "ONE ENTRY POINT" rule:
   - All provider access goes through `providers/index.js` only
   - `providers/nvidia.js` adapts from `src/services/ai-providers/nemotron.js`
   - No direct imports of nemotron.js from outside providers/

3. **Agent Pipeline Order** (for swarm):

   ```
   planner → cto → database → backend → frontend → testing → reviewer
   ```

4. **What NOT to Build** (V2.0 Traps):
   - ❌ Do NOT build AI API integrations that lock to one provider
   - ❌ Do NOT try to "run" agents FROM Ultra-Dex (orchestrate, not execute)
   - ❌ Do NOT compete with Claude Code, Cursor, Devin
   - ❌ Do NOT add web dashboard before CLI is stable
   - ❌ Do NOT build team features before solo developer workflow is proven

**Status: V1.x COMPLETE ✅ | V2.0 Phase 1 Ready to Begin**
