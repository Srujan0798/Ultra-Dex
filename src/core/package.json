{
  "name": "@ultra-dex/core",
  "version": "6.0.0",
  "description": "Core logic for the Ultra-Dex Meta-Layer",
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "build": "mkdir -p ../dist && echo 'Core build: ESM modules ready from src/core'"
  }
}
