# V1.3 - Logging Spine Protocol

**Source:** UD_2 Audit Analysis  
**Agent:** Qwen CLI + Gemini CLI  
**Priority:** P2 (After execution works)  
**Duration:** 45 minutes

---

## OBJECTIVE
Create single unified logging system. Destroy fragmentation.

---

## CURRENT PROBLEM (FROM UD_2/UD_4)

You have **5 parallel logging systems**:
```
1. monitoring logs
2. analytics logs
3. governance logs
4. dashboard logs
5. console.* calls (1813 total)
```

This is NOT observability. This is **log noise**.

---

## TARGET STATE

```
CLI / Agents / Runtime
        ↓
   logger.js (SINGLE SPINE)
        ↓
Structured Logs (JSON)
        ↓
[ stdout | file | memory | telemetry ]

All sources become SUBSCRIBERS, not separate sources.
```

---

## PART A: VERIFY LOGGER EXISTS

### Agent: Any
### Duration: 5 minutes

### Commands
```bash
# Check logger location
find apps/cli -name "logger.js" -o -name "logger.ts" 2>/dev/null

# Check logger content
cat apps/cli/lib/ui/logger.js 2>/dev/null | head -30
cat apps/cli/lib/utils/logger.js 2>/dev/null | head -30
```

### Expected
Logger file exists with structured logging support.

---

## PART B: DEFINE LOGGER CONTRACT

### Agent: Gemini CLI
### Duration: 15 minutes

### Prompt (if logger needs enhancement)
```
TASK: Ensure logger.js has proper structure

FILE: apps/cli/lib/utils/logger.js (or create if missing)

REQUIRED INTERFACE:
```javascript
import chalk from 'chalk';

const LOG_LEVELS = {
  debug: 0,
  info: 1,
  warn: 2,
  error: 3,
  success: 1
};

let currentLevel = LOG_LEVELS.info;

export const logger = {
  setLevel(level) {
    currentLevel = LOG_LEVELS[level] || LOG_LEVELS.info;
  },

  debug(message, meta = {}) {
    if (currentLevel <= LOG_LEVELS.debug) {
      this._log('debug', message, meta);
    }
  },

  info(message, meta = {}) {
    if (currentLevel <= LOG_LEVELS.info) {
      this._log('info', message, meta);
    }
  },

  warn(message, meta = {}) {
    if (currentLevel <= LOG_LEVELS.warn) {
      this._log('warn', message, meta);
    }
  },

  error(message, meta = {}) {
    this._log('error', message, meta);
  },

  success(message, meta = {}) {
    if (currentLevel <= LOG_LEVELS.info) {
      this._log('success', message, meta);
    }
  },

  _log(level, message, meta) {
    const timestamp = new Date().toISOString();
    const entry = {
      timestamp,
      level,
      message,
      ...meta
    };

    // Console output with colors
    const prefix = this._getPrefix(level);
    const metaStr = Object.keys(meta).length > 0 
      ? ` ${chalk.gray(JSON.stringify(meta))}` 
      : '';
    
    console.log(`${prefix} ${message}${metaStr}`);
  },

  _getPrefix(level) {
    switch (level) {
      case 'debug': return chalk.gray('[DEBUG]');
      case 'info': return chalk.blue('[INFO]');
      case 'warn': return chalk.yellow('[WARN]');
      case 'error': return chalk.red('[ERROR]');
      case 'success': return chalk.green('[SUCCESS]');
      default: return chalk.white('[LOG]');
    }
  }
};

export default logger;
```

CONSTRAINT:
- Keep it simple
- No external dependencies except chalk
- Structured metadata support
```

---

## PART C: MIGRATE CRITICAL FILES

### Agent: Qwen CLI
### Duration: 20 minutes

### Prompt
```
TASK: Replace console.* with logger.* in ONLY these 4 files

SCOPE (STRICT - NO OTHER FILES):
1. apps/cli/lib/commands/commit.js
2. apps/cli/lib/learn.js
3. apps/cli/src/wasm/index.js
4. apps/cli/src/wasm/runtime.js

RULES:

1. Add import at top:
import { logger } from '../utils/logger.js';
// OR adjust path based on file location

2. Replace console calls:
console.log("message") → logger.info("message", { module: "commit" })
console.log(colors.info(...)) → logger.info(...)
console.log(colors.warning(...)) → logger.warn(...)
console.log(colors.success(...)) → logger.success(...)
console.log(colors.subtle(...)) → logger.debug(...)
console.error(...) → logger.error(...)
console.warn(...) → logger.warn(...)

3. Add context metadata:
logger.info("Starting commit", { 
  module: "commit",
  step: "init"
});

CONSTRAINT:
- Do NOT change any logic
- Do NOT touch other files
- ONLY replace logging calls
- Keep user-facing output visible

OUTPUT:
- Count of replacements per file
- Any console.* that could NOT be replaced
```

### Validation
```bash
# Should return 0
grep -c "console\." apps/cli/lib/commands/commit.js 2>/dev/null || echo "0"
grep -c "console\." apps/cli/lib/learn.js 2>/dev/null || echo "0"
```

---

## PART D: WASM LOG BRIDGE

### Agent: Qwen CLI
### Duration: 10 minutes

### Prompt
```
TASK: Route WASM logs to JS logger

FILES:
- apps/cli/src/wasm/index.js
- apps/cli/src/wasm/runtime.js

RULE:
- No console.* inside WASM boundary
- All logs must flow through logger.js

IMPLEMENTATION:
1. Import logger (adjust path)
2. Replace any console.error with logger.error
3. Create bridge function if needed:

function wasmLog(level, message, meta = {}) {
  logger[level](message, { ...meta, source: 'wasm' });
}

OUTPUT:
WASM execution visible in main log stream
```

---

## PART E: VERIFY NO FRAGMENTATION

### Agent: Any
### Duration: 5 minutes

### Commands
```bash
# Count console.* in critical files (should be 0)
for f in apps/cli/lib/commands/commit.js apps/cli/lib/learn.js apps/cli/src/wasm/index.js apps/cli/src/wasm/runtime.js; do
  if [ -f "$f" ]; then
    count=$(grep -c "console\." "$f" 2>/dev/null || echo "0")
    echo "$f: $count console calls"
  fi
done

# Count logger usage (should be > 0)
grep -c "logger\." apps/cli/lib/commands/commit.js 2>/dev/null || echo "0"
```

---

## SUCCESS CRITERIA

```
[ ] logger.js exists and exports logger object
[ ] logger has: info, warn, error, debug, success methods
[ ] commit.js uses logger (0 console.*)
[ ] learn.js uses logger (0 console.*)
[ ] wasm files use logger (0 console.*)
[ ] Structured metadata included in logs
```

---

## LOG FORMAT STANDARD

Every log must look like:
```json
{
  "timestamp": "2026-04-03T06:45:00.000Z",
  "level": "info",
  "message": "Planning started",
  "run_id": "run_abc123",
  "agent": "planner",
  "module": "execution",
  "step": 1
}
```

---

## DO NOT

```
❌ Migrate all 1813 console calls
❌ Redesign logger architecture
❌ Add external logging libraries
❌ Change output visibility for user
❌ Touch files outside the 4 specified
```

---

## AFTER THIS PROTOCOL

Move to: **v1.4-trace-system.md**

---

*Single spine. All logs unified.*
