# Ultra-Dex Core Rules (v6.0.0)

> The foundational ruleset for the Meta-Layer. Load first.

## Meta-Layer Philosophy

<<<<<<< Current (Your changes)
- **Autonomy First**: Build for the autonomous Ralph Loop execution.
- **Verification Driven**: Protocol 21 is mandatory for all commits.
- **Relational Memory**: Every decision MUST be logged to the Cold Memory.
- **1000% Efficiency**: Optimize for reasoning-heavy AI models (DeepSeek-R1, o1).

## Code Standards

- **TypeScript strict mode** always.
- **Zod validation** at ALL boundaries.
- **Error handling**: No silent failure; use `AppError` primitives.
- **Logging**: Mandatory structured JSON via `src/core/utils/output.js`.
- **Tests**: Minimum **90% coverage** for business logic.

## Architecture Defaults

- **Structure**: Monorepo (apps/*, packages/*, src/*).
- **Core**: ES Modules only.
- **Security**: Mandatory Docker Sandbox for untrusted code execution.
=======
- Autonomy First: Build for the autonomous Ralph Loop execution.
- Verification Driven: Protocol 21 is mandatory for all commits.
- Relational Memory: Every decision MUST be logged to the Cold Memory.
- 1000% Efficiency: Optimize for reasoning-heavy AI models (DeepSeek-R1, o1).

## Code Standards

- TypeScript strict mode always.
- Zod validation at ALL boundaries.
- Error handling: No silent failure; use `AppError` primitives.
- Logging: Mandatory structured JSON via `src/core/utils/output.js`.
- Tests: Minimum **90% coverage** for business logic.

## Architecture Defaults

- Structure: Monorepo (apps/*, packages/*, src/*).
- Core: ES Modules only.
- Security: Mandatory Docker Sandbox for untrusted code execution.
>>>>>>> Incoming (Background Agent changes)

## Task Completion Checklist (Quick 5-Step)

1. Does it work? (Manual test)
2. Are there tests? (Automated)
3. Is it secure? (No secrets exposed, inputs validated)
4. Is it documented? (Code comments for complex logic)
5. Is it deployable? (No breaking changes)

## When to Use Full 21-Step

- New features affecting multiple files
- Security-sensitive changes
- Database schema changes
- API contract changes

## File Naming

- Components: PascalCase (`UserProfile.tsx`)
- Utilities: camelCase (`formatDate.ts`)
- API routes: kebab-case (`/api/user-profile`)
- Database: snake_case (`user_profiles`)

## Before/After Examples

### Example 1 (Fintech) - Bad -> Good
```typescript
// BAD: No validation or structured logging for transfers
export async function POST(req: Request) {
  const body = await req.json();
  const transfer = await createTransfer(body);
  return Response.json(transfer);
}
```

```typescript
// GOOD: Validate input and log structured event
import { z } from 'zod';

const transferSchema = z.object({
  amount: z.number().positive(),
  toAccountId: z.string().uuid(),
});

export async function POST(req: Request) {
  const data = transferSchema.parse(await req.json());
  const transfer = await createTransfer(data);

  console.log(JSON.stringify({
    level: 'info',
    event: 'transfer_created',
    amount: data.amount,
    toAccountId: data.toAccountId,
  }));

  return Response.json({ data: transfer }, { status: 201 });
}
```

### Example 2 (Healthcare) - Bad -> Good
```typescript
// BAD: Unstructured audit logs for patient access
console.log(`User ${userId} viewed patient ${patientId}`);
```

```typescript
// GOOD: Structured audit log with request ID
console.log(JSON.stringify({
  level: 'info',
  event: 'patient_record_viewed',
  userId,
  patientId,
  requestId,
}));
```
