name: 'Ultra-Dex AI Orchestration'
description: 'Run Ultra-Dex governance and agent review steps on pull requests and scheduled checks'
author: 'Ultra-Dex Team'
branding:
  icon: 'cpu'
  color: 'blue'
inputs:
  task:
    description: 'Specific task description for the AI agent'
    required: false
  provider:
    description: 'AI Provider to use (openai, anthropic, google)'
    required: false
    default: 'openai'
  model:
    description: 'Model to use for the selected provider'
    required: false
    default: 'gpt-4o'
  agents:
    description: 'Comma-separated list of agents to run (code-reviewer,security-audit,test-generator)'
    required: false
    default: 'code-reviewer,security-audit'
  config-path:
    description: 'Path to Ultra-Dex config file'
    required: false
    default: '.ultra-dex.json'
  auto-approve:
    description: 'If true, marks run as auto-approvable when all checks pass'
    required: false
    default: 'false'
  github-token:
    description: 'GitHub token for PR comment posting'
    required: false
  api-key:
    description: 'Legacy input kept for compatibility'
    required: false
  task:
    description: 'AI task description'
    required: false
  provider:
    description: 'AI provider (e.g. openai, anthropic)'
    required: false
  model:
    description: 'AI model to use'
    required: false
outputs:
  results:
    description: 'JSON summary of executed agents and checks'
  passed:
    description: 'Whether all checks passed'
runs:
  using: 'node20'
  main: 'index.js'
